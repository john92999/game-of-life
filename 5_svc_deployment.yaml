apiVersion: v1
kind: Service
metadata:
  labels:
    app: apache2-service
  name: apache2-svc
  namespace: web
spec:
  type: NodePort
  selector:
    app: apache2
    ports:
      - nodePort: 30080
        targetPort: 80
        port: 83
